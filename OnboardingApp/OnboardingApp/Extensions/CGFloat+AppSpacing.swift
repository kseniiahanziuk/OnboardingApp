import UIKit

extension CGFloat {
    // MARK: - Spacing values for reuse
    // (not every is needed, but I think it's ok to define the standard ones(for further development, not just this onboarding flow))
    static let spacingXXS: CGFloat = 4
    static let spacingXS: CGFloat = 8
    static let spacingS: CGFloat = 12
    static let spacingM: CGFloat = 16
    static let spacingXM: CGFloat = 20
    static let spacingL: CGFloat = 24
    static let spacingXL: CGFloat = 32
    static let spacingXXL: CGFloat = 40
    static let spacingXXXL: CGFloat = 48
}
